QSlider::groove:horizontal {
  height: 6px;
  background: rgba(0,0,0,0.10);
  border-radius: 3px;
}
QSlider::handle:horizontal {
  width: 14px;
  height: 14px;
  margin: -4px 0;             /* centre the handle on the groove */
  border-radius: 7px;
  background: #00889C;
  border: 1px solid rgba(0,0,0,0.15);
}